# 配置热重载修复计划

## 目标

解决 WebUI “配置已保存”与运行状态不一致的问题。最终需要保证：

1. 保存成功不等于假装已经生效；接口和界面必须明确返回“已生效、下轮生效、等待检测结束、需要重启”。
2. 正在运行的检测始终使用同一份配置快照，不在中途混用新旧参数。
3. Cron、Sub-Store 等长期任务由各自的运行管理器负责重配，不能由配置回调直接进入阻塞循环。
4. 89 个唯一界面配置键都有明确的生效策略和自动化测试。
5. 不修改或停止 Sparkle/Mihomo；正式部署只重启 subs-check-pro，Cloudflare Tunnel 保持运行。

## 执行结果（2026-07-28）

五个批次均已实现、构建并部署。当前运行核心为
`v2.6.8+custom.history.ua2.loopback1.cleantags1.silentarchive1.remotefilter1.cronreload1.configstate1.substore1.addrout1.configsem1.subrace1`。

- 配置运行态为 `applied`，期望版本与已应用版本一致，没有待重启或下次启动字段。
- 实际运行调度器报告 Cron 为 `0 5,19 * * *`，下次执行时间为 `2026-07-28T19:00:00+08:00`。
- Sub-Store 由单一异步管理器托管，运行态为 `running`，期望版本与已应用版本一致。
- WebUI 共 89 个唯一表单控件、96 个完整 YAML 字段路径，全部进入机器可读生效策略和覆盖测试。
- 新核心 SHA256 为 `E35EF630C97F299910CDBE4EB349279DB6371B29DA7E3E9D30927D91D1C6B8FD`；Race 修复部署前备份位于 `runtime/backups/substore-mailbox-race-20260729-0825/`。
- 完整构建、筛选后的 Go 测试、100 轮 Sub-Store 并发压力测试、补丁干净应用和 `scripts/verify.ps1` 均通过。
- 已在 `F:\tools\msys64` 安装 MSYS2 UCRT64 GCC 16.1.0；Race Detector 发现并修复 Sub-Store 单槽邮箱并发死锁。修复后 Sub-Store 专项 Race 20 轮、完整 `app` Race 5 轮及 `proxy`、`check` Race 均通过。
- 真实 Cron 已于 2026-07-28 19:00 和 2026-07-29 05:00 两次准时启动；05:00 这一轮于 07:05 完成，下一次为当天 19:00。

## 设计原则

### 配置分成四类

| 类型 | 处理方式 | 典型字段 |
|---|---|---|
| 立即生效 | 原子发布后，下一次请求立即读取 | `api-key`、`share-password` |
| 下轮生效 | 当前检测保留旧快照，下一轮使用新快照 | 检测、订阅、存储、通知参数 |
| 托管服务重配 | 交给调度器或子进程管理器异步 reconcile | Cron、Sub-Store 端口/路径/Cron/Push |
| 需要重启 | 保存配置，但明确显示尚未应用 | `listen-port`；无法安全动态切换的启动项 |

`enable-web-ui` 单独处理：路由始终注册，通过动态访问门控制；关闭前界面提示会失去管理入口，响应返回后再应用。

### 运行配置不再直接覆盖

- 配置文件解析到新的不可变对象，校验成功后才发布。
- 当前检测启动时取得深拷贝快照，直到该轮结束都使用这份快照。
- 代理探测结果、Sub-Store 状态等运行态数据与用户配置分离，禁止再回写用户配置对象。
- 配置差异由统一应用器计算，不在文件监听回调中逐字段堆叠特殊逻辑。

## 修复批次

### 第一批：建立安全边界（P0）

范围：

- 增加配置版本号、配置深拷贝和统一加载/校验入口。
- 检测开始时固定本轮配置快照。
- 检测期间保存的新配置进入 `pending-next-run`，本轮不切换。
- API 返回配置版本及 `applied/pending/restart-required` 状态。
- 将文件监听和 WebUI 保存统一到同一条应用路径，避免重复或乱序重载。

验收：

- 人工构造一个持续运行的检测，中途修改超时、并发、过滤、存储参数；本轮报告只出现旧配置，下一轮只出现新配置。
- 并发保存不能触发 Go 数据竞争、死锁或部分字段更新。
- 配置解析失败时，磁盘错误内容不能覆盖当前运行配置。

用户影响：

- 检测过程中仍可保存配置，但界面会显示“已保存，下轮检测生效”。
- 不会再出现同一轮检测前半段用旧值、后半段用新值。

### 第二批：重做 Sub-Store 生命周期（P0）

范围：

- 为 Sub-Store 建立独立 context 和单一管理协程，不再复用应用级 context。
- 配置回调只提交期望状态，禁止同步调用 `RunSubStoreService`。
- 管理器按“停止旧进程 → 等待退出 → 必要时 KillNode 兜底 → 启动新进程”串行执行。
- 以下任一字段变化都纳入 Sub-Store 运行规格并触发受控重启：
  - `sub-store-port`
  - `sub-store-path`
  - `sub-store-sync-cron`
  - `sub-store-produce-cron`
  - `sub-store-push-service`
  - `mihomo-overwrite-url` 的本地/远程代理环境变化
- 检测正在使用 Sub-Store 时，可将重启推迟到安全点；界面显示等待原因。

验收：

- 保存接口在 2 秒内返回，不因 Node 长期运行而阻塞。
- 连续快速修改 10 次配置，最终只保留一个 Node 子进程和一个监督协程。
- 修改端口、路径、Cron、Push 后，运行规格与配置版本一致。
- 禁用、重新启用和端口冲突均有确定状态，不产生孤儿进程。

用户影响：

- 修改 Sub-Store 配置不再卡住“保存中”。
- 新 Cron/Push 配置会真正进入运行中的 Node 子进程。

### 第三批：修复监听地址、输出目录和现有脏数据（P1）

范围：

- 增加唯一的监听地址解析器，正确支持：
  - `8199`
  - `:8199`
  - `127.0.0.1:8199`
  - IPv6 host:port
- 所有内部 URL 统一使用解析出的数值端口和 IPv4 回环，不再手工字符串拼接。
- `listen-port` 保存后标记 `restart-required`，当前 HTTP 服务继续监听旧地址，避免手机连接和 Tunnel 突然断开。
- `output-dir` 的静态文件、分享文件、分析报告处理器在每次请求时解析当前已应用目录，不缓存启动目录。
- 精确迁移运行产物中的重复主机名内部 URL：
  - 只处理 `http://127.0.0.1:127.0.0.1:<port>/...` 这类确定错误的内部记录。
  - 不清空正常订阅健康历史和沉默订阅档案。
  - 重新生成 Sub-Store 配置中的 `subUserinfo/subInfoUrl`。

验收：

- 四种监听地址格式生成相同、合法的内部 URL。
- 修改 `output-dir` 后，新结果和 HTTP 下载路由同时切换到新目录。
- 修改 `listen-port` 后界面明确显示“重启后生效”，运行状态仍报告实际旧端口。
- 运行目录不再包含重复主机名 URL，正常历史计数不丢失。

用户影响：

- Sub-Store 流量信息恢复。
- 不会再出现结果写入新目录、手机却下载旧目录文件的分裂状态。

### 第四批：修复 WebUI 保存语义（P1）

范围：

- 删除 `github-token` 的双控件旧值覆盖：保留一个数据源，另一处显示引用或双向绑定同一状态。
- 保存响应展示每类字段的真实状态：
  - 已立即生效
  - 下轮生效
  - 等待服务重配
  - 需要重启
- `api-key` 修改成功后安全切换当前会话密钥；失败时不清除旧会话。
- `enable-web-ui` 关闭操作在响应成功后应用，并明确提示关闭后如何恢复。
- 自动更新逻辑拆分为：
  - 启动时检查，只在真实启动流程执行。
  - 定时任务重配，只重建 Cron，不在保存配置时同步检查或重启程序。

验收：

- 依次访问两个包含 GitHub Token 的页签，修改任一入口后保存结果一致。
- 修改 API Key 后无需出现无解释的 401 循环。
- 修改更新 Cron 不会在保存请求中意外触发启动更新流程。
- 每种生效状态都有界面测试和接口测试。

用户影响：

- “保存成功”将准确说明什么时候真正生效。
- 不会因页签访问顺序把 Token 悄悄改回旧值。

### 第五批：全量回归、构建和部署（发布门禁）

范围：

- 建立 89 字段的机器可读策略清单；WebUI 字段、YAML 字段和运行策略缺一项即测试失败。
- 增加配置重载、并发保存、Sub-Store 重配、HTTP 路由和 WebUI 表单回归测试。
- 在支持 race detector 的环境运行配置并发测试。
- 生成新的核心/WebUI 补丁，加入 `scripts/build-custom-core.ps1`。
- 更新 `scripts/verify.ps1`，验证配置版本、实际监听端口、调度器规格、Sub-Store 规格和待重启状态。

发布前检查：

1. 备份当前核心、配置文件及统计目录。
2. 确认没有检测正在运行。
3. 构建新核心并完成筛选后的本地测试。
4. 只重启 subs-check-pro；不停止 cloudflared、Sparkle 或 Mihomo。
5. 验证手机 WebUI、当天下一次 Cron、Sub-Store、订阅下载、分析报告和日志脱敏。
6. 做一次小规模检测，再做一次“检测中保存配置”的专项验收。

回滚：

- 配置格式保持向后兼容，不做不可逆迁移。
- 部署失败时恢复上一版 EXE 并重启 subs-check-pro。
- 脏数据迁移前保留原文件副本；只在新版本验收后删除备份。

## 实施顺序和完成定义

| 顺序 | 批次 | 是否允许进入下一批 |
|---|---|---|
| 1 | 配置快照和统一应用器 | P0 并发/快照测试全部通过 |
| 2 | Sub-Store 管理器 | 无阻塞保存、无重复 Node、运行规格一致 |
| 3 | 地址/目录/数据修复 | 内部 URL 与下载目录专项验收通过 |
| 4 | WebUI 真实状态 | 表单和 API 状态测试通过 |
| 5 | 发布门禁与部署 | 完整 verify、手机端和小规模检测通过 |

不能为了赶进度跳过第一批直接修某个字段；否则会继续累积新的特殊回调，并保留检测中途混用配置这个根因。
